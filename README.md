# CI/CD Workflow

This project uses a GitHub Actions workflow to run basic CI tasks.

## Validation

You can validate this workflow locally using `actionlint`:

```bash
actionlint .github/workflows/ci.yml
```
